# Phase 0 — Boot Blockers & First-Request Crashes

## Checklist

| # | File | What Was The Issue | Resolution | Status |
|---|------|--------------------|------------|--------|
| 1 | src/configuration/settings.py | `from configuration.settings import Settings` was importing the same module while it was being loaded, causing a circular import error before the app could start | Removed the import line entirely. The `Settings` class is already defined in the same file so no import is needed | ✅ Fixed |
| 2 | src/configuration/settings.py | `main.py` referenced `settings.api_host`, `settings.api_port`, `settings.api_reload` but these fields were never defined in the `Settings` class, causing an `AttributeError` at startup | Added the three fields with safe defaults: `api_host = "127.0.0.1"`, `api_port = 8000`, `api_reload = True` | ✅ Fixed |
| 3 | src/configuration/__init__.py | The package initializer was named `__init___.py` (3 underscores). Python does not recognize this as a valid package initializer, so the `src/configuration` folder was not treated as a package | Renamed the file to `__init__.py` (2 underscores each side) | ✅ Fixed |
| 4 | src/configuration/__init__.py | `boot_settings` is a method on the `Settings` class, not a module-level function. Exporting it from `__init__.py` caused an `ImportError`. Also `__all__` was missing a comma, silently concatenating the two strings | Replaced `boot_settings` with `Settings` in both the import line and `__all__`, and added the missing comma | ✅ Fixed |
| 5 | src/crew/crewmanager.py | `ConfluenceService` called `CrewManager(session_id=session_id)` but the constructor required `agent_config_path` as the first positional argument, causing a `TypeError` on the first request | Made `agent_config_path` optional in the constructor signature: `def __init__(self, session_id=None, agent_config_path=None)` | ✅ Fixed |
| 6 | src/configuration/settings.py | `_conversation_memory()` referenced `settings.memory_storage_path` to create local session folders, but this field was not defined in the `Settings` class, causing an `AttributeError` at runtime | Added `memory_storage_path: str = "./memory_storage"` to the `Settings` class | ✅ Fixed |
| 7 | src/crew/crewmanager.py | In `boot_tasks()`, three tasks (reasoning, memory sync, response) were stored in `task_configuration` (the YAML data dict) instead of the `tasks` runtime dict. This also used a self-referencing assignment pattern that caused a `NameError` | Separated the YAML read and the Task creation into two lines each, storing all tasks correctly in `tasks[...]` | ✅ Fixed |
| 8 | src/crew/crewmanager.py | In `get_crew_instance()`, the task list referenced `task_retrieve_over_evidence` (non-existent key) instead of `task_reasoning_over_evidence`, and had `["task_format_response"]` (a plain list) instead of a Task object, causing a `KeyError` and `TypeError` | Fixed the key name and replaced the plain list with `self.tasks["task_format_response"]` | ✅ Fixed |
| 9 | src/crew/crewmanager.py | `get_crew_instance()` created the `Crew` object but never returned it, so the caller always received `None` and `.kickoff()` would fail with `AttributeError` | Added `return crew_instance` after the `Crew(...)` initialization | ✅ Fixed |
| 10 | src/crew/crewmanager.py | Agent was stored as `orchestation_agent` (typo, missing 'r') in `boot_agents()` but referenced as `orchestration_agent` in `boot_tasks()` and `get_crew_instance()`, causing a `KeyError` | Standardized all occurrences to `orchestration_agent` | ✅ Fixed |
| 11 | requirements.txt | `crewai_tools` package was imported in `crewmanager.py` for `MCPServerAdapter` but was not listed in `requirements.txt`, causing an unresolved import error on clean installs | Added `crewai-tools` to `requirements.txt` | ✅ Fixed |
| 12 | src/api/routes.py | `except Exception` was catching intentional `HTTPException` (400/401/404) and re-raising them as 500, losing the original error meaning for the client | Added `except HTTPException: raise` before the generic `except Exception` block in `/analyze`, `/generate-word-document`, and `/download-word-document` endpoints | ✅ Fixed |
